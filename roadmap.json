{
  "$schema_version": "1.0",
  "project": "cert_viewer",
  "last_updated": "2026-04-26",
  "priorities": "1) Testing & coverage \u2192 2) Cross-platform trust-store discovery \u2192 3) Distribution & installers \u2192 4) Toolchain & quality gates (align to updated ~/.claude/languages/go.md)",
  "notes": [
    "Phase 1 (Foundation) and most of Phase 2 (platform trust stores, CI/release pipeline), Phase 3 (core features), and Phase 4 (advanced features) completed in 2026-Q1 \u2014 see docs/roadmaps/2026-Q1.md.",
    "Pre-existing ROADMAP.md is retained read-only until this migration is verified."
  ],
  "references": [
    {
      "area": "2026-Q1 archive",
      "style_doc": "docs/roadmaps/2026-Q1.md",
      "state": "archived"
    },
    {
      "area": "Project guide",
      "style_doc": "CLAUDE.md",
      "state": "current"
    }
  ],
  "sections": [
    {
      "id": "2",
      "title": "Platform Support & Distribution",
      "description": "Installer/bundle outputs and per-OS build guides for end users.",
      "docs": [],
      "prerequisites": []
    },
    {
      "id": "3",
      "title": "Trust Store Discovery",
      "description": "Detect and surface non-standard / user-added root CAs without hard-coded paths or filesystem scanning. Driven by the discovery that homelab-ca was installed via update-ca-certificates but not surfaced because local_roots.json was stale.",
      "docs": [],
      "prerequisites": []
    },
    {
      "id": "4",
      "title": "Toolchain & Quality Gates",
      "description": "Bring the project into alignment with the updated ~/.claude/languages/go.md standards: blocking lint, race detection, vulnerability scanning, HTTP timeouts, reproducible toolchain pinning, and pre-commit hooks. Each task is sized to a single reviewable PR and includes its own documentation updates so CLAUDE.md, README.md, and BUILD_GUIDE_*.md never drift behind the code.",
      "docs": [],
      "prerequisites": []
    }
  ],
  "tasks": [
    {
      "id": "2.1",
      "section": "2",
      "title": "macOS: .app bundle and .dmg disk image",
      "description": "Produce distributable .app bundle and .dmg for macOS.",
      "type": "infra",
      "priority": "high",
      "effort": "medium",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Bundle binary as .app",
        "Package .app into .dmg"
      ],
      "acceptance_criteria": [
        "Signed .app bundle opens on macOS",
        ".dmg contains the .app and drag-to-Applications affordance"
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "migration",
      "completion_summary": "Migrated from ROADMAP.md as already-complete.",
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "2.2",
      "section": "2",
      "title": "Windows: installer via NSIS or WiX (or go-msi)",
      "description": "Produce a Windows installer for end users.",
      "type": "infra",
      "priority": "high",
      "effort": "medium",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Choose installer toolchain (NSIS/WiX/go-msi)",
        "Produce installer in CI"
      ],
      "acceptance_criteria": [
        "Installer runs on supported Windows versions",
        "Uninstall removes all installed files"
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "migration",
      "completion_summary": "Migrated from ROADMAP.md as already-complete.",
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "2.3",
      "section": "2",
      "title": "Linux: AppImage for distro-agnostic distribution",
      "description": "Build an AppImage so Linux users can run cert_viewer without per-distro packaging.",
      "type": "infra",
      "priority": "medium",
      "effort": "medium",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Configure AppImage tooling",
        "Produce AppImage in CI"
      ],
      "acceptance_criteria": [
        "AppImage runs on Debian/Ubuntu and Fedora without extra dependencies"
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "migration",
      "completion_summary": "Migrated from ROADMAP.md as already-complete.",
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "2.4",
      "section": "2",
      "title": "BUILD_GUIDE_LINUX.md",
      "description": "Debian/Ubuntu build guide: apt dependencies, go test, binary + AppImage.",
      "type": "infra",
      "priority": "medium",
      "effort": "small",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Document apt prerequisites",
        "Document go test and binary build commands",
        "Document AppImage packaging"
      ],
      "acceptance_criteria": [
        "A new contributor can build the binary and AppImage on a fresh Ubuntu install by following the guide"
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "migration",
      "completion_summary": "Migrated from ROADMAP.md as already-complete.",
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "2.5",
      "section": "2",
      "title": "BUILD_GUIDE_WINDOWS.md",
      "description": "Windows build guide: TDM-GCC/MSYS2, cross-compile from Linux or native Windows build, NSIS installer.",
      "type": "infra",
      "priority": "medium",
      "effort": "small",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Document TDM-GCC/MSYS2 setup",
        "Document cross-compile and native build paths",
        "Document NSIS installer build"
      ],
      "acceptance_criteria": [
        "A new contributor can produce a Windows installer by following the guide"
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "migration",
      "completion_summary": "Migrated from ROADMAP.md as already-complete.",
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "2.6",
      "section": "2",
      "title": "BUILD_GUIDE_MACOS.md",
      "description": "macOS build guide: Xcode CLT, go test, binary + .app bundle + .dmg.",
      "type": "infra",
      "priority": "medium",
      "effort": "small",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Document Xcode CLT setup",
        "Document go test and binary build",
        "Document .app bundle and .dmg packaging"
      ],
      "acceptance_criteria": [
        "A new contributor can produce .app and .dmg on macOS by following the guide"
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "migration",
      "completion_summary": "Migrated from ROADMAP.md as already-complete.",
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "3.A",
      "section": "3",
      "title": "Cache freshness + standard env-var overrides for Linux trust store",
      "description": "Two small fixes that together would have surfaced the user's homelab CA today: (1) regenerate local_roots.json when the underlying bundle is newer than the cache, and (2) honor SSL_CERT_FILE / SSL_CERT_DIR env vars (the same overrides Go's crypto/x509 honors) so users who point their toolchain at a custom bundle get consistent behavior in cert_viewer. Today needsRegen() in internal/resources/localroots.go only triggers on legacy-format JSON, and localroots_linux.go hard-codes /etc/ssl/certs/ca-certificates.crt.",
      "type": "enhancement",
      "priority": "high",
      "effort": "small",
      "status": "done",
      "dependencies": [],
      "steps": [
        "In internal/resources/localroots.go, change needsRegen() to also return true when bundle source mtime is newer than the cache mtime. Pass the source path through from collectRoots() so EnsureLocalRootsJSON can stat it.",
        "In internal/resources/localroots_linux.go, before falling back to defaultLinuxBundle, check SSL_CERT_FILE; if set and readable, use it as the bundle source. If unset, check SSL_CERT_DIR (colon-separated) and concatenate PEMs from each directory.",
        "Record the actual resolved source path in localRootsFile.SourcePath so the cache can be invalidated correctly when the env var changes between runs.",
        "Add table-driven tests in localroots_linux_test.go covering: (a) cache regen when bundle mtime advances, (b) SSL_CERT_FILE override picked up, (c) SSL_CERT_DIR override with multiple .pem files merged, (d) precedence order SSL_CERT_FILE > SSL_CERT_DIR > default.",
        "Update CLAUDE.md 'Runtime Paths' table to note that SSL_CERT_FILE / SSL_CERT_DIR override the default bundle."
      ],
      "acceptance_criteria": [
        "Touching /etc/ssl/certs/ca-certificates.crt (or running update-ca-certificates) causes the next 'Compare Local vs CCADB' to regenerate the cache without manual deletion.",
        "Setting SSL_CERT_FILE=/path/to/custom.pem before launch causes that bundle's roots to be used instead of the system default, and certs in it appear in 'Compare Local vs CCADB'.",
        "Setting SSL_CERT_DIR=/dir1:/dir2 enumerates all *.pem / *.crt files across both directories.",
        "Existing localroots_linux_test.go and localroots_test.go suites still pass; new tests cover the 4 cases above.",
        "homelab CA (CN=pki, fingerprint 0B:A6:6E:43:F8:21:A3:AE:AF:9C:21:46:32:F9:98:06:DF:EA:7F:81:28:94:68:6D:8F:C9:4F:6F:EF:2B:58:C4) appears in local_roots.json on this workstation after the fix runs."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "opus-4-7:520c991c",
      "completion_summary": "Added platform-specific resolveTrustSource()/trustSourceMTime(); Linux honors SSL_CERT_FILE then SSL_CERT_DIR (colon-separated dirs of *.pem/*.crt/*.cer) then defaults to /etc/ssl/certs/ca-certificates.crt; cache regenerates when source identifier changes or source mtime exceeds cache mtime; macOS/Windows return ok=false for mtime (no single stat-able file). Verified locally: homelab-ca now appears in cache (147 roots vs 146 stale).",
      "branch": "feature/trust-store-cache-and-env-overrides",
      "commit": "b03ebae",
      "tests_added": 9
    },
    {
      "id": "3.B",
      "section": "3",
      "title": "Trust Sources view: per-distro source dirs + per-user NSS DBs (Linux)",
      "description": "Move beyond reading a single concatenated bundle to a 'Trust Sources' view that enumerates each origin separately and labels each cert with where it came from. Linux-first scope: detect distro family via /etc/os-release, enumerate the appropriate user-added source directory (e.g. /usr/local/share/ca-certificates/ on Debian, /etc/pki/ca-trust/source/anchors/ on RHEL, /etc/ca-certificates/trust-source/anchors/ on Arch, /etc/pki/trust/anchors/ on SUSE), and read per-user NSS DBs (~/.pki/nssdb, ~/.mozilla/firefox/*/cert9.db). macOS keychain split (System vs login vs SystemRoots) and Windows CurrentUser+GPO are deliberately deferred to follow-up tasks under this section to keep scope tractable.",
      "type": "feature",
      "priority": "medium",
      "effort": "large",
      "status": "done",
      "dependencies": [
        "3.A"
      ],
      "steps": [
        "Add internal/resources/distro_linux.go that parses /etc/os-release and returns a DistroFamily enum (debian, rhel, arch, suse, alpine, unknown) plus the matching anchor directory path.",
        "Add internal/resources/anchors_linux.go that enumerates *.crt / *.pem files in the family-appropriate anchor directory and returns []TrustSourceEntry tagged with origin = 'distro-anchor-dir'.",
        "Add internal/resources/nssdb_linux.go that locates ~/.pki/nssdb and ~/.mozilla/firefox/*/cert9.db and shells out to certutil -L -d sql:<path> if available, returning []TrustSourceEntry tagged with origin = 'nss-user' or 'nss-firefox'. Skip gracefully if certutil is not on PATH.",
        "Extend LocalRootSummary with an Origin field and an OriginPath field; update writeLocalRoots() to merge entries from the system bundle, env-var overrides, anchor dir, and NSS DBs into one deduplicated list keyed by SHA-256 fingerprint.",
        "Add a 'Trust Sources' tab in internal/ui/advanced/ (or a new sub-package) that lists each origin with its cert count, expandable to show per-cert subject + SKI + which CCADB bucket (or 'not in CCADB') the cert falls into. Reuse TightTwoColLayout for the per-cert detail rows.",
        "Add table-driven tests in internal/resources/ for: distro detection from sample os-release fixtures, anchor-dir enumeration with mixed .crt/.pem files, NSS DB parsing with a stubbed certutil. Use t.Setenv to redirect HOME and t.TempDir() for fixtures.",
        "Document the new Trust Sources tab and per-origin labels in CLAUDE.md and README.md."
      ],
      "acceptance_criteria": [
        "On Debian/Ubuntu, certs from /usr/local/share/ca-certificates/ are listed under origin 'distro-anchor-dir' separately from the bulk of /etc/ssl/certs/ca-certificates.crt.",
        "On RHEL/Fedora, certs from /etc/pki/ca-trust/source/anchors/ are listed under origin 'distro-anchor-dir'.",
        "If certutil is installed and ~/.pki/nssdb exists, certs from it appear under origin 'nss-user'. If certutil is missing, the section shows a clear note rather than failing.",
        "Trust Sources tab shows totals per origin (e.g., 'system bundle: 148, distro-anchor-dir: 2, nss-user: 0, nss-firefox: 1') and lets the user expand each to inspect individual certs.",
        "Existing 'Compare Local vs CCADB' tab continues to work and now also surfaces the origin label for any cert it shows.",
        "All new code has unit tests; package coverage in internal/resources/ does not regress."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-25",
      "completed_by": "opus-4-7:520c991c",
      "completion_summary": "Linux Trust Sources view: per-distro anchor dir enumeration (Debian/RHEL/Arch/SUSE/Alpine via /etc/os-release ID + ID_LIKE), per-user NSS DB reads via certutil shellout (~/.pki/nssdb + Firefox profiles), LocalRootSummary extended with Origins []OriginRef, mergeTrustEntries() dedup by SHA-256, new top-level 'Trust Sources' tab in main.go with per-origin accordions and CCADB membership badges, Compare Local vs CCADB rows now show 'Origins:' line. Resources package coverage 82.8% \u2192 85.8%. Verified locally: homelab CA tagged system-bundle+distro-anchor-dir; mkcert tagged across all four origins. macOS keychain split and Windows CurrentUser/GPO deferred per task description.",
      "branch": "feature/trust-sources-view",
      "commit": "d9c7036",
      "tests_added": 28
    },
    {
      "id": "4.A",
      "section": "4",
      "title": "Add .golangci.yml with required linter set; wire into CI as blocking",
      "description": "The CI workflow at .github/workflows/ci.yml currently runs only `go vet`. The updated Go guide requires a blocking golangci-lint step with a specific linter set. This is the single highest-leverage change in section 4 because several enabled linters (errcheck, errorlint, bodyclose, contextcheck, noctx) will surface real existing issues \u2014 most notably the swallowed `_ = os.Remove(...)` and `_ = prefs.Save(p)` errors in internal/resources/fetcher.go.",
      "type": "infra",
      "priority": "high",
      "effort": "medium",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Add .golangci.yml at repo root enabling at minimum: errcheck, govet, staticcheck, gosec, revive, gocritic, bodyclose, errorlint, contextcheck, noctx, nilerr. Set `run.timeout: 5m` and `issues.exclude-use-default: false`. Pin a specific golangci-lint version (e.g. v1.62.x) for reproducibility.",
        "Run `golangci-lint run ./...` locally and triage the resulting findings into two buckets: (a) fix-now (clear bugs or trivial fixes \u2014 e.g. swallowed errors in fetcher.go) and (b) deferred-with-//nolint (each suppression must carry a justification comment).",
        "Add a `lint` job to .github/workflows/ci.yml using `golangci/golangci-lint-action@v6` with the same pinned version. Run it in parallel with the existing `test` job. Use `args: --timeout=5m` and ensure exit code is blocking.",
        "Update CLAUDE.md 'Code Conventions Used in This Project' section to list the lint config as the source of truth and to mention `golangci-lint run` as a required pre-commit check.",
        "Update each BUILD_GUIDE_*.md to document `golangci-lint run` in the local-development command list (alongside `go test ./...`)."
      ],
      "acceptance_criteria": [
        ".golangci.yml exists at repo root and lists every required linter from the updated guide.",
        "CI fails on a freshly introduced lint violation (verify with a throwaway commit during PR review).",
        "All swallowed-error sites flagged by errcheck in internal/resources/fetcher.go are either fixed or carry a `//nolint:errcheck // <reason>` comment.",
        "CLAUDE.md and the three BUILD_GUIDE_*.md files reference `golangci-lint run` as part of the standard local workflow.",
        "Existing `go vet` step in ci.yml is removed (subsumed by golangci-lint's govet linter) or kept with a comment explaining why."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-26",
      "completed_by": "opus-4-7:0f3afb9c",
      "completion_summary": "Added .golangci.yml with required linter set (errcheck, staticcheck, gosec, revive, gocritic, bodyclose, errorlint, contextcheck, noctx, nilerr, plus ineffassign/unused/misspell). Wired blocking lint job to ci.yml (golangci-lint-action@v6, pinned v1.62.2). Fixed 5 real bugs surfaced: deferred conn.Close error, oidExtensionAuthorityKeyId naming, deprecated container.NewMax, dead runtime.GOOS check in linux-only file, context propagation through chain.BuildFromCerts. Documented gosec/gocritic categorical suppressions at rule level. Deferred revive doc-comment rules (~30 additions) to a separate cleanup task per CLAUDE.md tech debt entry. Updated CLAUDE.md and all three BUILD_GUIDE_*.md files with the new lint workflow.",
      "branch": "feature/golangci-lint-ci",
      "commit": "d349563",
      "tests_added": 0
    },
    {
      "id": "4.B",
      "section": "4",
      "title": "Enable -race in CI test step",
      "description": "The Go guide states race conditions caught at test time are vastly cheaper than in production. The current CI runs `go test ./...` without -race. EnsureCCADBCSV in internal/resources/fetcher.go spawns a goroutine that mutates a Preferences struct the caller may also touch \u2014 exactly the case -race exists to catch.",
      "type": "infra",
      "priority": "high",
      "effort": "small",
      "status": "done",
      "dependencies": [
        "4.A"
      ],
      "steps": [
        "Change the Test step in .github/workflows/ci.yml from `go test ./...` to `go test -race -count=1 ./...`. The `-count=1` defeats test caching, which interacts poorly with -race in CI.",
        "Run `go test -race ./...` locally first; triage and fix any data races that surface (likely candidates: shared mutation of prefs.Preferences inside EnsureCCADBCSV, any UI-thread vs goroutine state in main.go).",
        "If a race is genuinely unavoidable (e.g. third-party Fyne internals), document the suppression with a comment and a tracking note.",
        "Update CLAUDE.md 'Testing Strategy' section to require `go test -race ./...` before merging significant changes."
      ],
      "acceptance_criteria": [
        "CI runs `go test -race -count=1 ./...` and the run is green.",
        "No new ad-hoc //go:build !race exclusions are introduced unless individually justified in a comment.",
        "CLAUDE.md documents -race as the standard local test invocation."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-26",
      "completed_by": "opus-4:50f32f66",
      "completion_summary": "Enabled -race -count=1 in CI test step. Local run was clean \u2014 no data races to fix. CLAUDE.md Testing Strategy now documents -race as the merge-gate invocation.",
      "branch": "feature/http-timeouts-and-race",
      "commit": "ed42b85",
      "tests_added": 0
    },
    {
      "id": "4.C",
      "section": "4",
      "title": "Introduce shared *http.Client with explicit timeouts; replace http.DefaultClient",
      "description": "The Go guide explicitly calls out: 'Set timeouts on all http.Client and http.Server instances \u2014 defaults are unsafe (no timeout).' Today, internal/certs/crl_fetch.go:17, internal/certs/ocsp_check.go:45 and :89, and internal/resources/fetcher.go:31 and :120 all use `http.DefaultClient.Do(req)`. Context is correctly threaded through, but a misbehaving server that dribbles bytes forever will defeat context-only deadlines because there is no idle-read timeout. internal/certs/tls_fetch.go:63 already does the right thing for the TLS dial path; extend that pattern to HTTP.",
      "type": "enhancement",
      "priority": "high",
      "effort": "small",
      "status": "done",
      "dependencies": [],
      "steps": [
        "Add a small internal/httpclient package (or a package-private `httpClient = &http.Client{Timeout: 30 * time.Second}` in each of internal/certs and internal/resources \u2014 pick whichever the deep-module-architect agent recommends; a shared internal package is preferred so timeouts can be tuned in one place).",
        "Replace every `http.DefaultClient.Do(req)` call in internal/certs/crl_fetch.go, internal/certs/ocsp_check.go, and internal/resources/fetcher.go with the new shared client.",
        "Make the timeout values configurable via constants in the new package (e.g. `DefaultTimeout = 30 * time.Second`, `CCADBDownloadTimeout = 120 * time.Second` since the CSV is large) \u2014 do not pull from prefs yet; that is a follow-up if it ever becomes useful.",
        "Add a table-driven test using httptest.NewServer that simulates a hung response (writes one byte then sleeps) and asserts the client returns an error within the configured timeout window. One test per timeout class is sufficient.",
        "Update CLAUDE.md 'Code Conventions Used in This Project' to list the shared http client as the required pattern; add a one-liner explaining why http.DefaultClient is banned in this codebase."
      ],
      "acceptance_criteria": [
        "No remaining call sites of `http.DefaultClient` in internal/certs or internal/resources (verify with `grep -r 'http.DefaultClient' internal/`).",
        "Every HTTP-using function still accepts and threads a context.Context.",
        "New httptest-backed timeout test passes and demonstrates a 30s timeout actually fires.",
        "CLAUDE.md documents the convention and the rationale."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-26",
      "completed_by": "opus-4:50f32f66",
      "completion_summary": "Added internal/httpclient package with Default() (30s) and CCADBDownload() (120s) shared clients. Replaced all 6 http.DefaultClient.Do call sites in internal/ (certs/crl_fetch.go, certs/ocsp_check.go x2, resources/fetcher.go x2, ui/chain/chain.go). Added httptest hung-server timeout test that exercises the actual client.Timeout behavior plus singleton-identity and configured-value tests. CLAUDE.md updated to ban http.DefaultClient with rationale.",
      "branch": "feature/http-timeouts-and-race",
      "commit": "4eba603",
      "tests_added": 3
    },
    {
      "id": "4.D",
      "section": "4",
      "title": "Pin Go toolchain via go.mod toolchain directive",
      "description": "go.mod currently has only `go 1.24.0`. The updated guide says: 'Pin to a specific Go minor version per project in go.mod, not just a floor ... Update toolchain directive in go.mod when bumping versions.' This makes builds reproducible across machines that may have newer Go installed and tells agents exactly which stdlib features are available.",
      "type": "infra",
      "priority": "medium",
      "effort": "small",
      "status": "ready",
      "dependencies": [],
      "steps": [
        "Add `toolchain go1.24.0` line to go.mod immediately after the `go 1.24.0` line.",
        "Run `go mod tidy` and verify go.sum is unchanged (toolchain directive should not change the dependency graph).",
        "Run `go build ./cmd/cert_viewer` and `go test ./...` locally to confirm the build still works with the pinned toolchain.",
        "Update CLAUDE.md 'Quick Start' section to mention the pinned Go version requirement and to point at `rustup`-style language: contributors should use the version pinned in go.mod (golang.org/dl/go1.24.0 or similar).",
        "Update each BUILD_GUIDE_*.md to reference the toolchain directive when discussing Go installation."
      ],
      "acceptance_criteria": [
        "go.mod contains both `go 1.24.0` and `toolchain go1.24.0` lines.",
        "`go build ./...` and `go test ./...` succeed unchanged.",
        "CLAUDE.md and BUILD_GUIDE_*.md reference the pinned toolchain.",
        "CI's `actions/setup-go@v5 with: go-version-file: go.mod` continues to work (the action honors the toolchain directive)."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": null,
      "completed_by": null,
      "completion_summary": null,
      "branch": null,
      "commit": null,
      "tests_added": 0
    },
    {
      "id": "4.E",
      "section": "4",
      "title": "Add govulncheck job to CI",
      "description": "The Go guide implies dependency vulnerability scanning as part of the standard CI surface (analogous to `cargo audit` for Rust). govulncheck is the official Go tool for this and only flags vulnerabilities in code paths the project actually reaches, which keeps signal high.",
      "type": "infra",
      "priority": "medium",
      "effort": "small",
      "status": "done",
      "dependencies": [
        "4.A"
      ],
      "steps": [
        "Add a `vuln` job to .github/workflows/ci.yml that installs govulncheck (`go install golang.org/x/vuln/cmd/govulncheck@latest`) and runs `govulncheck ./...`. Pin to a specific govulncheck version once the workflow is stable.",
        "Run govulncheck locally first to baseline existing findings; if any CVEs are flagged, decide per-finding whether to upgrade the dependency, switch dependency, or document a deliberate accept-and-track entry.",
        "Make the job blocking by default. If a finding cannot be fixed quickly, gate the job behind a documented exception (e.g. `continue-on-error: true` with a tracked roadmap entry to remove it).",
        "Update CLAUDE.md 'Code Conventions Used in This Project' to mention `govulncheck ./...` as part of the local pre-PR workflow."
      ],
      "acceptance_criteria": [
        "CI has a `vuln` job that runs `govulncheck ./...` and is blocking on a clean baseline.",
        "Any deliberate-accept entries are documented in CLAUDE.md's 'Known Technical Debt' section with a link to the upstream issue.",
        "CLAUDE.md documents govulncheck in the standard local workflow."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-26",
      "completed_by": "opus-4:50f32f66",
      "completion_summary": "Added vuln CI job running govulncheck@v1.3.0 ./... \u2014 non-blocking (continue-on-error) pending 4.D toolchain pin since baseline shows 17 reachable Go stdlib CVEs (all fixed in 1.25.x patch releases) plus one transitive x/image finding via Fyne. CLAUDE.md updated: Code Conventions documents govulncheck in pre-PR workflow; Known Technical Debt #8 records the non-blocking exception with explicit removal trigger.",
      "branch": "feature/govulncheck-precommit",
      "commit": "62e81d9",
      "tests_added": 0
    },
    {
      "id": "4.F",
      "section": "4",
      "title": "Add pre-commit hooks for gofmt, goimports, golangci-lint",
      "description": "The Go guide recommends pre-commit hooks for `gofmt`, `goimports`, and `golangci-lint`. This catches the cheapest class of issues before CI runs. Use the `pre-commit` framework (https://pre-commit.com) since it is language-agnostic and already familiar across the user's other projects.",
      "type": "infra",
      "priority": "medium",
      "effort": "small",
      "status": "done",
      "dependencies": [
        "4.A"
      ],
      "steps": [
        "Add .pre-commit-config.yaml at repo root with hooks for gofmt (via `dnephin/pre-commit-golang` or equivalent), goimports, and `golangci-lint run --new-from-rev=main` (incremental, only checks changed files for speed).",
        "Pin each hook to a specific revision.",
        "Document `pre-commit install` as a one-time setup step in CLAUDE.md 'Quick Start' section.",
        "Add a one-line note in each BUILD_GUIDE_*.md pointing contributors at `pre-commit install` for local hook setup.",
        "Run the hooks locally against the current tree to confirm they pass cleanly."
      ],
      "acceptance_criteria": [
        ".pre-commit-config.yaml exists at repo root and includes the three required hooks pinned to specific revisions.",
        "`pre-commit run --all-files` succeeds against a clean main.",
        "CLAUDE.md and BUILD_GUIDE_*.md document the install step.",
        "Pre-commit hooks are advisory at the developer level \u2014 they do not change CI behavior (CI is the source of truth via 4.A and 4.B)."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": "2026-04-26",
      "completed_by": "opus-4:50f32f66",
      "completion_summary": "Added .pre-commit-config.yaml with gofmt + goimports (dnephin/pre-commit-golang@v0.5.1) and golangci-lint (local hook, --new-from-rev=main for incremental commit-time runs). pre-commit run --all-files passes clean. Cleanup commit corrected pre-existing gofmt drift in 11 files (map literal alignment, no behavior change). CLAUDE.md Quick Start and all three BUILD_GUIDE_*.md document pre-commit install. Hooks are advisory; CI remains the blocking authority.",
      "branch": "feature/govulncheck-precommit",
      "commit": "0538fd3",
      "tests_added": 0
    },
    {
      "id": "4.G",
      "section": "4",
      "title": "Adopt log/slog as the project logging standard (forward-looking)",
      "description": "The Go guide prefers `log/slog` (Go 1.21+) for new code. The project barely logs today (it's a GUI app), so this task is small and forward-looking: pick the convention now so future logging additions don't fragment between `log.Printf` and `slog`.",
      "type": "enhancement",
      "priority": "low",
      "effort": "small",
      "status": "backlog",
      "dependencies": [
        "4.D"
      ],
      "steps": [
        "Audit existing logging call sites with `grep -rn '\\\"log\\\"\\|log\\.Print\\|fmt\\.Println' cmd/ internal/`. There should be very few, if any.",
        "Replace any non-test `log.*` or `fmt.Println` debug-style calls with `slog` equivalents (use `slog.Default()` configured in main.go).",
        "In cmd/cert_viewer/main.go, configure a default `slog.Handler` early in main() \u2014 text handler in dev (controlled by an env var like CERT_VIEWER_LOG=debug), JSON handler is overkill for a desktop GUI but should be the documented option for future.",
        "Document the convention in CLAUDE.md 'Code Conventions Used in This Project' so future code uses slog and not log/log15/zerolog."
      ],
      "acceptance_criteria": [
        "No remaining `log.Printf` / `log.Println` / `fmt.Println` calls in non-test code (verify with grep).",
        "main.go configures a `slog` default handler before any package-init logging could fire.",
        "CLAUDE.md documents slog as the project logging standard."
      ],
      "assigned_agent": null,
      "assigned_at": null,
      "lock_expires": null,
      "archived": false,
      "created_at": "2026-04-25",
      "completed_at": null,
      "completed_by": null,
      "completion_summary": null,
      "branch": null,
      "commit": null,
      "tests_added": 0
    }
  ]
}
